# URL of the backend API - no trailing slash.
# Prefer including /api. If omitted, the frontend appends it automatically.
#
# Local development can omit this file and use the Vite /api proxy.
# VITE_API_URL=http://localhost:5000/api
# VITE_API_URL=http://localhost:5000

# Production (Render backend):
VITE_API_URL=https://krishicre.onrender.com/api
